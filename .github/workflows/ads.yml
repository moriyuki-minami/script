name: auto_doreming_shussha

on: [ workflow_dispatch ]

#on:
#  schedule:
#   - cron: '0 0 * * 1,2,3,4,5'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: read csv
        run: |
          pwd
          csv=`cat ./holidays.csv`

      - name: echo csv
        run: |
          echo "$csv"
